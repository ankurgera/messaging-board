<nav class="bg-blue-500 h-10 text-white">
  <div class="flow-root">
      <div class="float-left mt-1 text-2xl ml-2">
        Messaging Board
      </div>  
      <div class="float-right mt-2 mr-2">
        <% if user_signed_in? %>
          Logged in as <%= current_user.full_name %> |
          <%= link_to 'Edit Profile', edit_user_registration_path, class: "text-white ml-1 no-underline"%> |
          <%= link_to "Logout", destroy_user_session_path, data: { turbo_method: :delete }, class: "text-white ml-1 no-underline" %>
        <% end %>  
      </div>
  </div>
</nav>
